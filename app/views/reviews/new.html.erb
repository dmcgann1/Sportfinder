<!-- May need to do nested routes to get the below working -->

<%= form_for [@facility, @review] do |f| %>
  <div class="field">
    <%= f.label "Add a new review" %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
