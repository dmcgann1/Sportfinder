<%= form_for [@facility, @review] do |f| %>
  <div class="field">
    <%= f.label "Edit review" %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
